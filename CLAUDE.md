## Project Rules

1. Always build reusable React components instead of duplicating code.

2. Validate all user inputs before allowing form submission.

3. Ensure every form is responsive and accessible using proper labels and ARIA attributes.